//import { useEffect } from "react";
//import apiRoutes from "../../config/apiRoutes";
//import useCreateUrl from "../../hooks/url";

export interface CreateUrlInterface {
  url: string;
}

export default function CreateUrl({ url }: CreateUrlInterface) {
  console.log(url);
  return null;
}
